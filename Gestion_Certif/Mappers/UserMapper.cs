using Gestion_Certif.Model;
using Gestion_Certif.ViewModels;

namespace Gestion_Certif.Mappers
{
    public class UserMapper
    {
        /*public static User FromUserVMToUser(AuthVM user)
        {
            if (user == null)
            {
                throw new ArgumentNullException(nameof(user));
            }

            User userr;
            switch (user.)
            {
                case "Manager":
                    userr = new Manager
                    {
                       
                        password = user.password,
                        email = user.email

                    };
                    break;
                case "Collaborator":
                    userr = new Collaborateur
                    {
                       
                        password = user.password,
                        email = user.email

                    };
                    break;
                default:
                    throw new ArgumentException("Invalid user role specified", nameof(user));
            }

            return userr;
        }*/
    }
}
